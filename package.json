{
  "name": "myApp",
  "private": true,
  "scripts": {
    "start": "meteor --settings settings.json --port $IP:$PORT"
  },
  "dependencies": {
    "babel-runtime": "^6.25.0",
    "meteor-node-stubs": "~0.2.4",
    "simpl-schema": "^0.3.2"
  }
}
